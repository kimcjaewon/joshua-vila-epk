# Joshua Vila — EPK

Single-page electronic press kit for Joshua Vila (Filipino R&B singer-songwriter, Los Angeles).
Static site — just `index.html` + `/images`. No build step.

## Deploy
Hosted on Vercel, source on GitHub. Push to `main` and Vercel auto-deploys.

## Edit
All content lives in `index.html`. Photos in `/images`.
